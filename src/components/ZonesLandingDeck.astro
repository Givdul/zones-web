---
import ZonesDemo from "./ZonesDemo.astro";
import ZonesEditorDemo from "./ZonesEditorDemo.astro";
import type { AppRecord } from "../data/site";

interface Props {
  app: AppRecord;
}

const { app } = Astro.props;
const pricingModel = app.pricingModel ?? "freemium";
const isTrialThenPaid = pricingModel === "trial_then_paid";
---

<div class="zones-landing">
  <main
    class="givdul-app-landing zones-landing__main"
    data-motion-root
    aria-label="Zones for Mac"
  >
    <section
      id="zones-hero"
      class="zones-landing__section zones-landing__section--hero"
      aria-label="Overview"
    >
      <div class="zones-landing__inner">
        <section class="quiet-orbit-hero">
          <div class="quiet-orbit-hero__copy">
            <h1 data-reveal data-reveal-delay="0">{app.heroTitle}</h1>
            <p class="quiet-orbit-hero__tagline" data-reveal data-reveal-delay="75">{app.heroTagline}</p>
            <p class="quiet-orbit-hero__lede" data-reveal data-reveal-delay="150">{app.heroLede}</p>
            <div class="quiet-orbit-hero__actions" data-reveal data-reveal-delay="220">
              {app.hasAppStoreUrl ? (
                <a
                  class="app-store-badge app-store-badge--hero"
                  href={app.appStoreUrl!}
                  target="_blank"
                  rel="noreferrer"
                  aria-label={`Download ${app.name} on the App Store`}
                >
                  <img src="/download-on-the-mac-app-store.svg" alt="Download on the App Store" />
                </a>
              ) : (
                <span class="button button--catalog button--disabled" aria-disabled="true">App Store link pending</span>
              )}
            </div>
            <div class="quiet-orbit-hero__workflow" data-reveal data-reveal-delay="290">
              {app.workflowSteps.map((step, index) => (
                <>
                  {index > 0 && <span class="workflow-step__arrow" aria-hidden="true"></span>}
                  {step.kind === "key" ? (
                    <span class="workflow-step workflow-step--key">
                      <kbd>{step.label}</kbd>
                    </span>
                  ) : (
                    <span class="workflow-step">
                      <span class="workflow-step__icon" aria-hidden="true">
                        {step.icon === "window" ? (
                          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="2" y="2" width="12" height="12" rx="2" stroke="currentColor" stroke-width="1.5"/></svg>
                        ) : (
                          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M8 2v12M2 8h12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
                        )}
                      </span>
                      <span class="workflow-step__label">{step.label}</span>
                    </span>
                  )}
                </>
              ))}
            </div>
          </div>

          <div class="quiet-orbit-hero__stage quiet-orbit-hero__stage--abstract" data-reveal data-reveal-delay="140">
            <ZonesDemo />
          </div>
        </section>
      </div>
    </section>

    <section
      id="zones-layouts"
      class="zones-landing__section zones-landing__section--showcase"
      aria-labelledby="zones-layouts-heading"
    >
      <div class="zones-landing__inner">
        <section class="zones-showcase" aria-labelledby="zones-layouts-heading">
          <div class="zones-showcase__copy" data-reveal data-reveal-delay="0">
            <span class="zones-showcase__eyebrow">Custom layouts</span>
            <h2 id="zones-layouts-heading">Slice your own bento, zone by zone.</h2>
            <p class="zones-showcase__lede">
              Click in the editor to place a vertical slice. Shift-click for a horizontal slice. Drag
              an existing slice to move it. Build as deep a grid as you want, then toggle individual
              zones on or off.
            </p>
            <p class="zones-showcase__lede zones-showcase__lede--muted">
              In Zones, everything is a layer: preset grids, corner zones, and your custom bento
              are all layers you can turn on or off wholesale or fine-tune zone by zone. Run only
              custom targets, only presets, or any mix — you decide what stays live.
            </p>
          </div>
          <div class="zones-showcase__stage" data-reveal data-reveal-delay="120">
            <ZonesEditorDemo />
          </div>
        </section>

        <section class="quiet-orbit-proof" aria-label="Key product points">
          {app.proofPoints.map((point, index) => (
            <div class="quiet-orbit-proof__item" data-reveal data-reveal-delay={String(index * 90)}>
              <div class="proof-item__icon" aria-hidden="true">
                {point.icon === "window" && (
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                    <rect x="2" y="3" width="16" height="14" rx="2" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M2 7h16" stroke="currentColor" stroke-width="1.5"/>
                    <circle cx="5" cy="5" r="1" fill="currentColor"/>
                  </svg>
                )}
                {point.icon === "gesture" && (
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                    <path d="M10 4v12M4 10h12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                  </svg>
                )}
                {point.icon === "star" && (
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                    <path d="M10 2l2.5 5.5L18 8l-4 4 1 5.5L10 14.5 5 17.5l1-5.5-4-4 5.5-.5L10 2z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/>
                  </svg>
                )}
              </div>
              <div class="proof-item__content">
                <strong>{point.title}</strong>
                <span>{point.copy}</span>
              </div>
            </div>
          ))}
        </section>
      </div>
    </section>

    <section
      id="zones-pricing"
      class="zones-landing__section zones-landing__section--pricing"
      aria-label="Pricing"
    >
      <div class="zones-landing__inner">
        <section class="quiet-orbit-choices" aria-labelledby={`${app.slug}-pricing-heading`}>
          <div class="quiet-orbit-choices__intro">
            <h2 id={`${app.slug}-pricing-heading`}>
              <span class="quiet-orbit-choices__headline" data-reveal data-reveal-delay="0">
                {app.pricingIntro?.headline ?? app.pricing[0]?.title ?? "Available soon."}
              </span>
              <span class="quiet-orbit-choices__subheadline" data-reveal data-reveal-delay="95">
                {app.pricingIntro?.subheadline ?? app.pricing[1]?.title ?? "More details later."}
              </span>
            </h2>
          </div>

          <div class:list={["quiet-orbit-cards", isTrialThenPaid && "quiet-orbit-cards--trial-then-paid"]}>
            {app.pricing.map((tier, index) => {
              const dividerLabel = isTrialThenPaid
                ? "Includes"
                : index === 0
                  ? null
                  : "Built to evolve in place:";
              const dividerIsSpacer = !isTrialThenPaid && index === 0;
              const usePlusIcon = !isTrialThenPaid && index > 0;
              const cardTone = tier.name === "free" ? "free" : "pro";

              return (
                <article
                  class:list={[
                    "orbit-card",
                    `orbit-card--${cardTone}`,
                    tier.emphasis === "recommended" && "orbit-card--recommended",
                  ]}
                  data-reveal
                  data-reveal-delay={String(index * 110)}
                >
                  <div class="orbit-card__header">
                    <div class="orbit-card__badge">
                      <span class="orbit-card__label">{tier.label}</span>
                    </div>
                    <h3>{tier.title}</h3>
                    <p class="orbit-card__price">
                      <span class="price-value">{tier.price}</span>
                      <span class="price-note">{tier.note}</span>
                    </p>
                    <p class="orbit-card__lead">{tier.lead}</p>
                  </div>

                  <div
                    class:list={["orbit-card__tier-divider", dividerIsSpacer && "orbit-card__tier-divider--empty"]}
                    aria-hidden={dividerIsSpacer ? "true" : undefined}
                  >
                    <span class:list={["tier-label", !dividerLabel && "tier-label--invisible"]}>
                      {dividerLabel ?? "Built to evolve in place:"}
                    </span>
                  </div>

                  <ul class="orbit-card__list">
                    {tier.items.map((item) => (
                      <li>
                        {usePlusIcon ? (
                          <svg class="plus-icon" width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                            <path d="M8 3v10M3 8h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
                          </svg>
                        ) : (
                          <svg class="check-icon" width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                            <path d="M3 8l3.5 3.5L13 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                          </svg>
                        )}
                        <span>{item}</span>
                      </li>
                    ))}
                  </ul>

                  {app.hasAppStoreUrl ? (
                    <a
                      class:list={["button", tier.name === "free" ? "button--free" : "button--pro"]}
                      href={app.appStoreUrl!}
                      target="_blank"
                      rel="noreferrer"
                    >
                      <span>{tier.cta}</span>
                      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                        <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                      </svg>
                    </a>
                  ) : (
                    <span
                      class:list={["button", tier.name === "free" ? "button--free" : "button--pro", "button--disabled"]}
                      aria-disabled="true"
                    >
                      {tier.cta}
                    </span>
                  )}
                </article>
              );
            })}
          </div>
        </section>
      </div>
    </section>
  </main>
</div>
